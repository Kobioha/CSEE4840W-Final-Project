#include "input.h"
#include "game.h"

uint16_t input_read(int frame) {
    uint16_t input = 0;

    if (frame < 80) input |= INPUT_RIGHT;
    else if (frame < 160) input |= INPUT_LEFT;

    if (frame % 20 == 0) input |= INPUT_FIRE;

    return input;
}
